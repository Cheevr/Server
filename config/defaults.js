module.exports = {
    tasks: {
        waitForComplete: true, // for interval jobs whether to wait for finish before calculating next run
        sleep: 0,   // Sleep between jobs, including a start up sleep time
    }
};
